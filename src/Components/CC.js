import React from 'react';
import {useSelector} from "react-redux";

const Cc = () => {
    const state=useSelector((state) =>state)
    return (
        <div>
            <h1>dsdjhsjdhjsh</h1>
        </div>
    );
};

export default Cc;
